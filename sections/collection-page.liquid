<section class="collection-page">
  <div class="container flex">
    <ul class="collections-menu side-menu">
      {% for collectionItem in section.settings.collection_list %}
        <li><a href="{{ collectionItem.url }}">{{ collectionItem.title }}</a></li>
      {% endfor %}
    </ul>

    {% render 'collection', collection_handle: collectionItem.handle, isDescription: true, isNoTitle: false, isNoPrices: false, isLinkToCollection: false, isCentered: customer.b2b?, isNoBundles: false, isLastCollectionRender: isLastCollectionRender %}
  </div>
</section>

{% schema %}
  {
      "name": "Collection Page",
      "disabled_on": {
        "groups": [
          "header",
          "footer"
        ]
      },
      "settings": [
        {
          "type": "collection_list",
          "id": "collection_list",
          "label": "Collections"
        }
      ],
      "presets": [
      {
        "name": "Collection Page"
      }
    ]
  }
  {% endschema %}