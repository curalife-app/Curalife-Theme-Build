<div class="subscription-benefits mt-2">
  <h3>Complimentary subscriber benefits:</h3>
  <ul class="benefits-list check">
    <li>Free gift in every box!</li>
    <li>Free Expert Consultations</li>
    <li>Best Price Guarantee</li>
  </ul>
  <a href="#" id="see-all-benefits" class="see-all-link">See all benefits</a>
</div>

<div id="benefits-modal" class="modal" aria-labelledby="modal-title" aria-hidden="true">
  <div class="modal-backdrop"></div>
  <div class="modal-content">
    <div class="close-icon" aria-label="Close modal">&times;</div>
    <h2 id="modal-title">Complimentary subscriber benefits:</h2>
    <div id="benefits-list">
      <div class="benefit">
        <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/subscription-benefits-icon-1.svg" alt="Free gift in every box!" class="icon">
        <div class="content">
          <h3>Free gift in every box!</h3>
          <p>Enjoy a special surprise with each purchase – a free gift just for you!</p>
        </div>
      </div>
      <div class="benefit">
        <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/subscription-benefits-icon-2.svg" alt="Free Expert Consultations" class="icon">
        <div class="content">
          <h3>Free Expert Consultations</h3>
          <p>Enjoy free chats with Naturopathic and Nutrition Doctors to tailor your health plan</p>
        </div>
      </div>
      <div class="benefit">
        <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/subscription-benefits-icon-3.svg" alt="Best Price Guarantee" class="icon">
        <div class="content">
          <h3>Best Price Guarantee</h3>
          <p>Enjoy unbeatable value with our best prices guaranteed all year round!</p>
        </div>
      </div>
      <div class="benefit">
      <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/subscription-benefits-icon-4.svg" alt="Cancel, Pause, Delay or Change, Hassle Free" class="icon">
      <div class="content">
          <h3>Cancel, Pause, Delay or Change, Hassle Free</h3>
          <p>Manage your subscription effortlessly with options to cancel, pause, delay, or change anytime, without any hassle.</p>
        </div>
      </div>
      <div class="benefit">
        <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/subscription-benefits-icon-5.svg" alt="Double & Triple Loyalty Points" class="icon">
        <div class="content">
          <h3>Double & Triple Loyalty Points</h3>
          <p>At Curalife, you earn loyalty points with every order, which you can use to purchase our products later. As a subscription user, you will enjoy double and triple loyalty points rewards compared to regular orders.</p>
        </div>
      </div>
    </div>
    <button class="close-button">Close</button>
  </div>
</div>

<style>
  .subscription-benefits {
    max-height: 450px;
    overflow: hidden;
    transition: max-height 0.5s ease-in-out, opacity 0.5s ease-in-out, margin-top 0.5s ease-in-out;
    opacity: 1;
    margin-top: 0.5rem;

    &.hidden {
      transition: max-height 0.5s ease-in-out, opacity 0.5s ease-in-out, margin-top 0.5s ease-in-out;
      max-height: 0;
      opacity: 0;
      margin-top: 0;
    }

    h3 {
      color: var(--primary-color);
      font-size: 1.1rem;
      margin-bottom: 0.75rem;
      font-weight: 600;
    }

    @media (max-width: 768px) {
      padding: 0.75rem;

      h3 {
        font-size: 1rem;
      }
    }
  }

  .benefits-list {
    list-style-type: none;
    padding: 0;
    margin: 0;

    li {
      display: flex;
      align-items: center;
      margin-bottom: 0.75rem;
      font-size: 16px;

      &::before,
      &::after {
        background: var(--primary-color);
        position: absolute;
      }

      &::before {
        top: 0 !important;
        background: #3CB4D0 !important;
      }

      &:first-child::before {
        content: '';
        background: var(--bronze) !important;
      }

      &:first-child::after {
        content: '';
        -webkit-mask-image: url("data:image/svg+xml,%3Csvg...%3E%3C/svg%3E");
        mask-image: url("data:image/svg+xml,%3Csvg...%3E%3C/svg%3E");
        -webkit-mask-size: cover;
        mask-size: cover;
        background: white;
        width: 10px;
        height: 10px;
        left: 5px;
        top: 5px;
      }
    }
  }

  .close-icon {
    position: absolute;
    top: 10px;
    right: 20px;
    font-size: 35px;
    background: none;
    border: none;
    cursor: pointer;
    color: var(--primary-color);
    transition: color 0.3s;

    &:hover {
      color: var(--primary-color-dark);
    }
  }

  .benefit-text {
    font-size: 0.9rem;
    color: var(--text-color);

    @media (max-width: 768px) {
      font-size: 0.8rem;
    }
  }

  .benefit-icon {
    @media (max-width: 768px) {
      font-size: 1.1rem;
    }
  }

  .see-all-link {
    display: inline-block;
    margin-top: 0.5rem;
    color: var(--primary-color);
    font-size: 0.9rem;
    text-decoration: underline;
    font-weight: 600;
    transition: color 0.2s ease-in-out;

    &:hover {
      color: var(--primary-color-dark);
    }
  }

  .modal {
    --text: #4a5568;
    --border: #e2e8f0;
    --bg: #fefefe;
    position: fixed;
    inset: 0;
    z-index: 1000;
    overflow: auto;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s, visibility 0.3s;

    &.show {
      opacity: 1;
      visibility: visible;

      .modal-backdrop {
        opacity: 1;
      }

      .modal-content {
        transform: translateY(0);
        opacity: 1;
      }

      .benefit {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .modal-content {
      background: var(--bg);
      margin: 3% auto;
      padding: 2rem 3rem;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      max-width: 600px;
      width: 90%;
      transform: translateY(-20px);
      opacity: 0;
      transition: transform 0.3s, opacity 0.3s;

      h2 {
        color: var(--primary-color);
        font-size: 1.5rem;
        margin-bottom: 1.25rem;
      }

      .benefit {
        display: flex;
        align-items: center;
        padding-bottom: 1rem;
        margin-bottom: 1rem;
        border-bottom: 1px solid var(--border);
        opacity: 0;
        transform: translateX(-20px);
        transition: opacity 0.3s, transform 0.3s;

        &:last-child {
          border-bottom: none;
          margin-bottom: 0;
          padding-bottom: 0;
        }

        .icon {
          font-size: 1.5rem;
          min-width: 60px;
          margin: 0;
          flex-shrink: 0;
          object-fit: scale-down;
          object-position: left;
        }

        .content {
          h3 {
            color: var(--primary-color);
            font-size: 1.125rem;
            margin: 0 0 0.3rem;
          }

          p {
            color: var(--text);
            font-size: 0.875rem;
            margin: 0;
          }
        }
      }

      .close-button {
        display: block;
        width: 30%;
        margin: auto;
        padding: 0.75rem;
        margin-top: 1.5rem;
        background-color: var(--primary-color);
        color: white;
        border: none;
        border-radius: 50px;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.3s;

        &:hover {
          background-color: var(--primary-color-dark);
        }
      }

      @media (max-width: 480px) {
        padding: 1rem;

        h2 {
          font-size: 1.5rem;
        }

        .benefit {
          .content {
            h3 {
              font-size: 1rem;
            }

            p {
              font-size: 0.8125rem;
            }
          }
        }

        .close-button {
          font-size: 0.9rem;
          padding: 0.6rem;
        }
      }
    }

    .modal-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.4);
      opacity: 0;
      transition: opacity 0.3s;
    }
  }

  #benefits-list {
    list-style: none;
    padding: 0;
  }

  .benefit-icon {
    font-size: 1.1rem;
  }

  .icon {
    font-size: 1.5rem;
    min-width: 60px;
    margin: 0;
    flex-shrink: 0;
    object-fit: scale-down;
    object-position: left;
  }

  @media (max-width: 480px) {
    h2 {
      font-size: 1.5rem;
    }

    .content {
      h3 {
        font-size: 1rem;
      }

      p {
        font-size: 0.8125rem;
      }
    }

    .close-button {
      font-size: 0.9rem;
      padding: 0.6rem;
    }
  }
</style>

<script>
  const modal = document.getElementById('benefits-modal');

  const toggleModal = show => {
    modal.classList.toggle('show', show);
    modal.setAttribute('aria-hidden', !show);
    document.body.style.overflow = show ? 'hidden' : '';

    if (show) {
      setTimeout(() => {
        document.querySelectorAll('.benefit').forEach((benefit, index) => {
          setTimeout(() => benefit.style.transition = 'opacity 0.5s, transform 0.5s', index * 100);
        });
      }, 300);
    }
  };

  const closeIcon = modal.querySelector('.close-icon');
  closeIcon.addEventListener('click', () => toggleModal(false));

  const closeButton = modal.querySelector('.close-button');
  closeButton.addEventListener('click', () => toggleModal(false));

  modal.addEventListener('click', event => {
    if (event.target === modal || event.target.classList.contains('modal-backdrop')) {
      toggleModal(false);
    }
  });

  document.getElementById('see-all-benefits').addEventListener('click', (event) => {
    event.preventDefault();
    toggleModal(true);
  });

  document.addEventListener('keydown', event => {
    if (event.key === 'Escape' && modal.classList.contains('show')) toggleModal(false);
  });
</script>