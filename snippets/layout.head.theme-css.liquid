<style>
  {{ settings.type_body_font | font_face: font_display: 'swap' }}
  {{ settings.body_font_bold | font_face: font_display: 'swap' }}
  {{ settings.body_font_italic | font_face: font_display: 'swap' }}
  {{ settings.body_font_bold_italic | font_face: font_display: 'swap' }}
  {{ settings.type_header_font | font_face: font_display: 'swap' }}

  :root {
    --primary: {{ page.metafields.custom.primary_color | default: product.metafields.custom.primary_color | default: "var(--curalin)" }};
  }

  .{% if shop.name == 'CuraLife Global' %}us{% else %}global{% endif %}{display: none !important}
  {% unless customer.b2b? %}.b2b{display: none !important}{% endunless %}
  .admin-hidden {display: none !important}
  body.admin .admin-hidden {display: block !important}

  #ReCharge {
    padding-top: 50px;
  }
  input[type="email"] {
    width: 375px;
    border: 1px solid black;
    padding: 1rem;
    margin-bottom: 10px;
  }

  #app0-webchat button {@media(width<768px){bottom: 90px}}

  #upCart #UpcartPopup p {font-size: 16px;}
</style>