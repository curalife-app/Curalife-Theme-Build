{% comment %}
	Quiz Web Components Configuration

	This snippet sets up the global configuration for quiz Web Components,
	including the correct Shopify CDN URL for the quiz.css file.

	Usage: {% render 'quiz-web-components-config' %}
	Usage with section: {% render 'quiz-web-components-config', section: section %}
{% endcomment %}

<script>
	// Set the correct Shopify CDN URL for quiz.css
	window.QUIZ_CSS_URL = '{{ 'quiz.css' | asset_url }}';

	// Optional: Set other quiz-related configuration
	window.QUIZ_CONFIG = {
	  cssUrl: '{{ 'quiz.css' | asset_url }}',
	  assetsPath: '{{ 'dummy' | asset_url | split: '/dummy' | first }}',
	  shopDomain: '{{ shop.permanent_domain }}',
	  debug: {% if section.settings.debug_mode %}{{ section.settings.debug_mode }}{% else %}{{ settings.quiz_debug_mode | default: false }}{% endif %}
	};

	{% comment %} Debug logging in development {% endcomment %}
	{% if section.settings.debug_mode or settings.quiz_debug_mode %}
	  console.log('🎯 Quiz Web Components Config:', window.QUIZ_CONFIG);
	  console.log('📍 Quiz CSS URL:', window.QUIZ_CSS_URL);
	{% endif %}
</script>
